在 MRT 中，「物理項 的衰退」直接對應到真實世界中的**黏滯性 (Viscosity)**。

我們剛剛把粒子分佈 拆解成了 。其中最重要的物理項（除了密度和動量）就是**應力矩 (Stress Moments)**，通常對應 D2Q9 矩陣中的第 7 和第 8 個分量：

- : ** (正應力)** — 流體被拉伸或壓縮的程度。
- : ** (切應力)** — 流體層與層之間滑動摩擦的程度（剪切）。

這兩個項的「衰退過程」，就是在模擬**流體如何透過「摩擦」來耗散能量**。

---

### 一、 衰退 (Relaxation) 的本質：回歸平靜

想像你攪動一杯咖啡，咖啡產生了漩渦（產生了應力 ）。當你停止攪動，咖啡會慢慢停下來，恢復平靜。

- **平靜的狀態**：這就是 **平衡態 ()**。對於應力來說，平衡態通常是 0（或者與速度梯度有關的某個值）。
- **慢慢停下來**：這就是 **衰退 (Relaxation)**。

在 LBM 的每一個時間步，我們都強迫這些應力值「往平衡態靠近一點點」：

如果我們假設平衡態 （為了簡化解釋，假設是純剪切流的擾動），公式就變成：

- ** (Relaxation Rate)**：衰退係數，範圍通常在 之間。
- \***\*：這就是**衰退因子 (Decay Factor)\*\*。

---

### 二、 衰退與黏度 () 的數學關係

這是最反直覺的地方，請仔細看：

**直覺想法**：黏度越高（像蜂蜜），阻力越大，應該衰退得越快吧？
**LBM 真相**：**剛好相反！**

根據 Chapman-Enskog 展開（一種數學推導，將 LBM 連結到 Navier-Stokes 方程），物理黏度 與衰退係數 的關係是：

或者反過來寫（求 ）：

讓我們代入數值來感受一下：

1. **高黏度 (蜂蜜)**：假設 很大（例如 10.0）。

- 。
- 衰退因子 。
- **物理意義**：應力 每一道手續只衰退 3%。它**保留了大部分的過去狀態**。
- **解釋**：高黏度流體有很強的「記憶性」，動量傳遞很遠，應力不容易消失。

2. **低黏度 (空氣/水)**：假設 很小（例如 0.005，你的建築風場案例）。

- 。
- 衰退因子 。
- **物理意義**：應力 每一道手續不僅衰退了，還**反向跳動**（Over-relaxation）。
- **解釋**：低黏度流體對應力的反應非常快，能量迅速轉化，幾乎不保留「記憶」。

---

### 三、 為什麼「空氣模擬」這麼危險？

回到你的專案。你要模擬建築風場（空氣），代表你要用 \*\*\*\*。

請看上面的衰退因子：\*\*\*\*。
這代表應力值會在正負之間劇烈震盪：

- : 應力 =
- : 應力 =
- : 應力 =
- : 應力 =

這種**接近 2.0 的劇烈震盪**，就是 LBM 數值不穩定的根源！

- 如果 超過 2.0，衰退因子變成 （例如 -1.1），震盪就會越來越大 **爆炸 (Blow up)**。
- BGK 因為只有這一個參數，所以一旦要模擬空氣，所有變數（包括噪音）都在劇烈震盪，很容易因為一點點浮點數誤差就超過邊界。

---

### 四、 MRT 如何拯救這個局面？

這裡就是 MRT 展現價值的地方。

在 BGK 中，我們被迫對「噪音（鬼影）」也使用同樣劇烈的 。噪音一旦震盪，馬上就炸了。

**在 MRT 中：**

1. **物理項 (應力 )**：

- 沒辦法，為了模擬空氣，我們必須設 。讓物理應力去震盪（這是物理真實的一部分，代表低黏度的湍流特性）。

2. **非物理項 (鬼影 )**：

- 我們**不需要**讓它跟著物理項一起發瘋。
- 我們把 設為 \*\*\*\*。
- 衰退因子 。
- **結果**：數值噪音每一道手續就縮小成原來的 10%。**噪音瞬間被殺死了。**

**結論：**
MRT 允許我們保留「物理上的劇烈反應（低黏度）」，同時「按住」數值噪音不讓它失控。這就是為什麼對於你的建築風場模擬，MRT 是絕對必要的。
